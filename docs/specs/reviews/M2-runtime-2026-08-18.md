# M2 Audit — Runtime lane

**日期**：2026-08-18  
**范围**：`services/runtime/**`  
**初判**：ACCEPT_WITH_FIXES → 返工 → **ACCEPT**

## 严重
无。

## 中等（已修）
| 项 | 处置 |
|----|------|
| four_crew packet 测试空洞 | 增 `test_four_crew_emits_packets` |
| `run.stage` 缺 `status` / 乱用 `done` | 发 `stage`+`status`（running/waiting_human/passed/rework） |
| four_crew 仅 ws_alpha | 任意 workspace 可 four_crew |
| bus 隔离未测 | 增 `test_bus_workspace_isolation` |

## 轻微（接受）
- SSE HTTP streaming 未在 TestClient 冒烟（总线历史覆盖主路径）
- `choice` action 后置
- dedupe 返回任意同 ws run（M2 mock 可接受）

## 检查表
| 项 | 结果 |
|----|------|
| FastAPI + 127.0.0.1:8787 | pass |
| org root+edges | pass |
| 信封 type/workspace_id/ts | pass |
| 禁 idle packet | pass |
| 命令对齐 11 | pass |
| ≥2 ws 隔离 | pass |
| CrewAI 依赖仍声明 + 投影单测 | pass |
| pytest | **13 passed, 1 skipped** |

## 结论：**ACCEPT**
