# M1 Audit — Living Org Canvas Mock

**日期**：2026-08-18  
**范围**：`apps/canvas/**` · `packages/protocol/**`  
**对照**：`docs/specs/m1-canvas-mock.md` A1–A9 · `08` · `10`§4.1 · `ssot/stack.md`  
**审计**：双 explore（product + eng）初判 **ACCEPT_WITH_FIXES** → Main 返工 → **ACCEPT**

---

## 严重 / 中等 / 轻微（修后）

### 严重
无。

### 中等（已修）
| 项 | 处置 |
|----|------|
| Bubbles 非节点锚定 | 改为 xyflow `nodeLookup` 绝对坐标锚定；不可见节点进 tray |
| Group 非区域框 | Group 作为 parent frame，子 seat `parentId` + 区域尺寸 |
| Hover 角落卡 | 锚定到 hover seat 下方 |
| `waiting_human` 原文 | 展示映射「等你」；协议枚举不变 |
| Badge 单测只测 helper | 增 `badgeFor.test.ts`（nested A4 + P0 waiting） |
| 无 `applyEvent`（M2 债） | 增 `store/applyEvent.ts`；handoff 经事件 apply |
| 未用 `motion` 依赖 | 移除；packet 用 SVG `animateMotion` + CSS |
| layout status 为 string | 收紧为 `SeatStatus` / `PacketPhase` |

### 轻微（接受或已顺手）
| 项 | 处置 |
|----|------|
| 角标多色同时显示 | 改为 §4.1 优先级 error > waiting > busy 单芯片 |
| review 步复用 eng→rev 边 | 保留；packet 仍标 `review`（文档化 mock） |
| Tailwind 几乎无 utility | 接受：token 管线 + 自定义 HUD CSS |

---

## 覆盖检查表

| 项 | 结果 |
|----|------|
| 目标 = Org Canvas 非聊天主 UI | **pass** |
| SeatStatus 与 10 一致 | **pass** |
| fixtures single/four/nested | **pass** |
| handoff brief→research→patch→review | **pass**（单测） |
| Dossier + prompt inject | **pass** |
| bubble approve/reject | **pass** |
| hover + 边高亮 + 双击 focus | **pass** |
| 栈锁 zustand / protocol / 无 pi | **pass** |
| badge §4.1 busy=tooling | **pass**（store 单测） |
| applyEvent M2 预备 | **pass** |
| typecheck / build / lint / test | **pass** |

### 验收 A1–A9

| ID | 结果 | 证据 |
|----|------|------|
| A1 build | **pass** | `pnpm --filter @ensemble/canvas build` |
| A2 single | **pass** | fixture |
| A3 four play | **pass** | TopBar + handoff test |
| A4 nested badge | **pass** | `badgeFor.test.ts` |
| A5 prompt | **pass** | Dossier |
| A6 approve | **pass** | resolveBubble + applyEvent |
| A7 lint | **pass** | eslint 无 error |
| A8 hover/focus | **pass** | 节点锚定 hover + focusUp |
| A9 label order | **pass** | handoff.test.ts |

### 命令复验（修后）

```text
typecheck  ✅
vitest     ✅  9 tests
build      ✅
lint       ✅
```

---

## 结论

### **ACCEPT**

M1 Living Org Canvas mock 满足验收与设计语言硬要求；审计刀口已关闭。  
**G1 门禁满足** → 可开 M2 并行（Runtime lane + Canvas wire），须另写 `gate-parallel-M2-*.md`。

### 优先修复（已完成，留档）
1. badgeFor 集成测试  
2. applyEnsembleEvent  
3. 节点锚定 bubble/hover + Group 区域框  
4. 状态展示文案  
5. 去 motion 死依赖  

### 残留风险（不阻塞 M1）
- Group 折叠时子节点隐藏，区域框高度策略在深树上仍需 M5 LOD  
- Bubble 锚定用 flow 绝对坐标，缩放/平移下需人工 spot-check  
- Tailwind utility 使用率低，M2 不必强行改写  

---

## 元数据

| 项 | 值 |
|----|-----|
| Product audit | ACCEPT_WITH_FIXES → fixed |
| Eng audit | ACCEPT_WITH_FIXES → fixed |
| Final | **ACCEPT** |
| Next | M2 Workspace + SSE（按 13 开闸） |
