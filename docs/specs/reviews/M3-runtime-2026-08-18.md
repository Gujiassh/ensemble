# M3 Audit — Runtime + minimal canvas wire

> **HISTORICAL / NOT CURRENT AUTHORIZATION.** This file records a superseded Tauri-era plan, implementation, review, or ownership result. Current Electron shell architecture and implementation ownership are defined by [m6-electron-shell.md](../m6-electron-shell.md) and [m6-interaction-implementation-slices.md](../m6-interaction-implementation-slices.md).

**日期**：2026-08-18  
**范围**：`services/runtime/**`、`runners/mock/**`、canvas Dossier Outputs/inject 最小接线、`packages/protocol` 事件类型  
**初判**：ACCEPT_WITH_FIXES → 返工 → **ACCEPT**

## 严重（已修）

| 项 | 处置 |
|----|------|
| HTTP `bubble.act(reject)` 在同步路由里 `asyncio.create_task` → `RuntimeError: no running event loop`，rework 不调度 | `bubble_act` / `human_inject` 改为 `async def`；registry 用 `get_running_loop().create_task`；增 `test_http_reject_rework_v2` |

## 中等（已修）

| 项 | 处置 |
|----|------|
| `bubble_act` 按 `ws_alpha` 硬绑 `seat_rev`，single_agent 会错绑 | `_bubble_seat` 从历史 `bubble.upsert` 取 seat |
| gate `state.json` 强制 `version: 1`，rework 后易丢版本语义 | 初轮 gate 写入当前 `version`；approve 落盘 `status=passed` |
| `start_run` 忽略 body.`prompt` | 传入 `prompt`/`prompt_text` |
| artifact GET 路径穿越风险 | basename 校验 + resolve 前缀检查 |
| canvas live SSE 未消费 `artifact.written` / `human.inject`；Outputs 仅文件名 | applyEvent + liveStore + Dossier 拉 `GET .../artifacts/{name}`；live inject API |

## 轻微（接受）

- SSE HTTP streaming 仍未单独 TestClient 冒烟（总线历史 + HTTP rework 覆盖主路径）
- M3-F08 roster `parent_id`：单 seat 写 `entries:[]`，schema 合法；L1 staffing 条目留给后续
- M3-F10 `staffing.proposed` 仍为 P1 未做
- four_crew reject 不触发 rework（仅 single_agent 模板）— 符合 M3 范围
- MockRunner 内容为占位 markdown，非真实 LLM

## 检查表

| 项 | 结果 |
|----|------|
| M3-F01 org load/save `tree.json` | pass |
| M3-F02 single_agent 1 seat (`ws_beta`) | pass |
| M3-F03 stage 状态机 + `run.stage` | pass |
| M3-F04 MockRunner 写 artifact | pass |
| M3-F05 契约失败可测 | pass (`test_contract_failure`) |
| M3-F06 approve done；reject → v2 | pass (`test_reject_rework_writes_v2`, HTTP) |
| M3-F07 timeline.jsonl | pass |
| M3-F08 roster.json schema | pass（空 entries） |
| M3-F09 human.inject 影响下一轮 mock prompt | pass |
| A1–A7 产品脚本 | pass（curl 文档在 runtime README） |
| A8–A9 CrewAI 依赖 + 投影不写 tree | pass |
| Org SSoT；Crew 只读投影 | pass |
| pytest | **19 passed, 1 skipped** |
| canvas typecheck/build/lint + vitest | **11 passed**；build green |

## 验收证据（命令）

```bash
cd services/runtime && . .venv/bin/activate && pytest -q
# 19 passed, 1 skipped

pnpm typecheck:canvas && pnpm --filter @ensemble/canvas test && pnpm build:canvas && pnpm lint:canvas
```

curl 一轮：见 `services/runtime/README.md` § M3 single-agent curl。

## 残留风险（进 M4）

- Tauri 进程监督 / 仅 127.0.0.1 / 关窗杀子进程未落地
- PiRunner 与真实 `pi` CLI 未接；无 pi 环境需 skip+标注
- four_crew 仍为时间轴 mock，非 CrewAI kickoff 真派发
- approve 后 canvas 未主动清空 bubble 以外的 run chrome（依赖事件）

## 结论：**ACCEPT**

下一刀：**M4 Tauri + PiRunner**（不另行请示）。
