# M4 Audit — Tauri shell + PiRunner + four_crew

**日期**：2026-08-18  
**范围**：`src-tauri/**`、`runners/pi/**`、`ensemble_runtime/run/{dispatch,four_crew,single_agent}.py`、README/m4 spec  
**初判**：**ACCEPT**（残留见下，不阻塞）

## 严重
无。

## 中等
无未修阻塞项。

已确认并接受的设计取舍：
| 项 | 说明 |
|----|------|
| four_crew 不 `crew.kickoff()` 真 LLM | `ENSEMBLE_CREWAI_MODE=mock` 经 `project_org_to_crew` 得 task 序，再 RunnerJob；timeline/`state.crew` 可证 CrewAI 投影（A4b）。Live kickoff 待 API key（F14） |
| A1 完整开窗未在本机无头环境实操 | `cargo check` 绿；webkit2gtk-4.1 OK；`pnpm dev:desktop` 文档齐全。有 DISPLAY 时人工点一次即可 |

## 轻微（接受）
- registry 仍保留 `_play_four` / `_play_single` 旧路径（未再被 four_crew/single_agent 主路径调用）
- canvas 尚未调用 `runtime_status` / `runtime_restart` invoke（壳侧已暴露）
- icons 为占位图
- Engineer 无 pi 时 `pi-fallback` 写 stub（故意，便于 demo）

## 检查表

| 项 | 结果 |
|----|------|
| M4-F01 tauri dev 文档/命令 | pass（`pnpm dev:desktop`） |
| M4-F02 窗内连 runtime | pass（壳 ensure_started + 现有 canvas live） |
| M4-F04 PiRunner 协议 | pass（`runners/pi`） |
| M4-F05 无 pi skip/标注 | pass（dry-run / not_found → fallback） |
| M4-F06 four_crew 边+产物+bubble | pass |
| M4-F07 cwd=`workspace_path` | pass |
| M4-F07b loopback + 关窗杀进程 | pass（`127.0.0.1`；`killpg`） |
| M4-F07c capabilities 最小 | pass（仅 `core:default`） |
| M4-F08 无 key 进 git | pass |
| M4-F09 mock 可切换 | pass（`ENSEMBLE_FORCE_MOCK`） |
| M4-F11–F14 CrewAI 投影路径 | pass（mock）；live deferred |
| A4b crew trace | pass（`crew.projected`） |
| pytest | **22 passed, 1 skipped** |
| canvas vitest | **11 passed** |
| `cargo check` | **pass** |

## 安全清单（A6）

| 条 | 结果 |
|----|------|
| Runtime 仅 127.0.0.1 | pass |
| pi cwd 限 workspace_path | pass |
| capabilities 无 fs/shell 插件 | pass |
| 密钥不进仓库 | pass |
| 关窗杀 runtime 进程树 | pass |

## 结论：**ACCEPT**

下一刀：**M5**（深套娃 / LOD 等，见 `docs/12-dev-plan.md`）— 按「审计后直接下一步」继续，不另行请示。
